import Vue from 'vue';
import Vuex from 'vuex';
import { workloads } from '../data';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    workloads,
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  },
});
